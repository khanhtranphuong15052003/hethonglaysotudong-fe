export interface Counter {
  id: string;
  name: string;
  number: number;
  isActive: boolean;
  processedCount: number;
}

export interface Service {
  id: string;
  name: string;
  code: string;
}
